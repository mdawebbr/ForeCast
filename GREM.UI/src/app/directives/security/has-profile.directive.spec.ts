import { HasProfileDirective } from './has-profile.directive';

describe('HasProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new HasProfileDirective();
    expect(directive).toBeTruthy();
  });
});
